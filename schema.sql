-- Drops the stella_dbif it exists currently --
DROP DATABASE IF EXISTS stella_db;
-- Creates the stella_db database --
CREATE DATABASE stella_db;
-- Use stella_db if created already --
USE stella_db;